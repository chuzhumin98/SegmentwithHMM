package myHMM;

public class testModel {
	
	/**
	 * 针对输入的句子进行分词处理
	 * 输出分词只有的结果，中间用空格隔开
	 * 
	 * @param input
	 * @return
	 */
	public String segment(String input) {
		ReadTrain train = ReadTrain.getInstance();
		int N = train.N;
		
		return input;
	}
}
